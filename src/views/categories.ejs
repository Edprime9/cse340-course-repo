  <%- include('partials/header') %>
    <main>
      <h1><%= title %></h1>
      <p>Here are a few of our Service Project by Categories:</p>
      <ul class="main-list">
        <% categories.forEach(category => { %>

            <li>
                <strong><%= category.name %></strong>
                -
                <%= category.description %>
            </li>
            <% }); %>
      </ul>
    </main>
    <%- include('partials/footer') %>
  </body>
</html>
